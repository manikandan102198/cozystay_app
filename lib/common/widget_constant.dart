import 'package:flutter/material.dart';

class WidgetConstant {
  static final kContainerDecoration = BoxDecoration(
      color: Colors.white,
      borderRadius: BorderRadius.circular(9.0),
      boxShadow: [
        BoxShadow(color: Colors.grey, blurRadius: 0.5, spreadRadius: 0.2)
      ]);
}
